from setuptools import setup, find_packages

setup(
    name="foundation_stereo",
    version="0.0.1",
    packages=find_packages(where="src"),
    author="",
    description=["Foundation Stereo"],
    url="",
)